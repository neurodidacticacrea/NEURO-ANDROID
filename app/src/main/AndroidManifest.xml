<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Audio is only used when the user actively starts voice recognition. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Required solely for the user-enabled floating NEURO brain. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground notification/service keeps the user aware that NEURO is active. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Used only if the user explicitly enables "Always available". -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Needed for secure AI backend communication. No API key lives in the APK. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Deliberately NOT requested:
         Accessibility, SMS, Contacts, Call Log, direct CALL_PHONE,
         broad storage/media read, device admin, notification listener. -->

    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.spotify.music" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:label="NEURO"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Neuro">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FloatingNeuroService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
